import { DetailsViewModel } from '../../../abstract/DetailsViewModel';
import { idKeys } from '../../../constants';

export class ContactDetails extends DetailsViewModel {
    constructor(...deps) {
        //@ts-ignore
        super(...deps);
    }
}