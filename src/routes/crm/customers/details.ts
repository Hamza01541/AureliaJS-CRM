import { DetailsViewModel } from "../../../abstract/DetailsViewModel";
import { idKeys } from "../../../constants";

export class CustomerDetails extends DetailsViewModel {
    constructor(...deps) {
        //@ts-ignore
        super(...deps);
    }
}