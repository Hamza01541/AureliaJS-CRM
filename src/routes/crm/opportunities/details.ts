import { DetailsViewModel } from "../../../abstract/DetailsViewModel";
import { idKeys } from "../../../constants";

export class OpportunityDetails extends DetailsViewModel {
    constructor(...deps) {
        //@ts-ignore
        super(...deps);
    }
}